# build output
/register
/register.exe
/storeregister
/storeregister.exe

# the live register data, never committed
store-register.json
store-register.json.bak
store-register.json.corrupt-*

# go
/vendor/
*.test
*.out

# Build output. The exe is shipped as a GitHub Release, never committed:
# git keeps every version forever and each one is ~10 MB.
/dist/

# The handoff packet is a verbatim dump of a working session -- it carries an
# email address, home directory paths and raw transcripts. The repository is
# public; that file is not for it.
.agent-handoff/
