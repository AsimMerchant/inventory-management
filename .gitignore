# build output
/register
/register.exe
/storeregister
/storeregister.exe

# the live register data, never committed
store-register.json
store-register.json.bak
store-register.json.corrupt-*

# go
/vendor/
*.test
*.out

# Build output. The exe is shipped as a GitHub Release, never committed:
# git keeps every version forever and each one is ~10 MB.
/dist/

# Never commit these. The repository is public.
# The ledger photos show real names and mobile numbers of real people, and
# the handoff packet is a verbatim dump of a working session.
IMG-*.jpg
IMG-*.jpeg
.agent-handoff/
